import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  output: 'export',
  basePath: '/https://github.com/Cerdint/GP-Test'
  // otras opciones de configuración aquí
};

export default nextConfig;
